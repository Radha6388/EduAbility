<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#67E49B">

    <!-- 🌤 TOP MESSAGE -->
    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Focus Time 🌿"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#2F5D62"
        android:layout_marginTop="30dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 🌱 PROGRESS CARD -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/progressCard"
        android:layout_width="0dp"
        android:layout_height="300dp"
        android:layout_margin="20dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/titleText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:background="#D7D0AE">

            <!-- 🌱 GROWING PLANT -->
            <ImageView
                android:id="@+id/progressImage"
                android:layout_width="140dp"
                android:layout_height="140dp"
                android:src="@drawable/plant_stage2"
                android:layout_marginBottom="10dp"/>

            <!-- TIMER -->
            <TextView
                android:id="@+id/timerText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="05:00"
                android:textSize="32sp"
                android:textStyle="bold"
                android:textColor="#2F5D62"/>

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- 🤖 BUDDY MESSAGE -->
    <TextView
        android:id="@+id/buddyMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="I'm here with you 😊"
        android:textSize="18sp"
        android:textColor="#082F35"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/progressCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 🎯 START BUTTON -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnStart"
        android:layout_width="220dp"
        android:layout_height="60dp"
        android:text="Start Focus 🎯"
        android:textSize="18sp"
        android:textColor="#100B0B"
        app:cornerRadius="30dp"
        android:backgroundTint="#2F5D62"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="40dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>